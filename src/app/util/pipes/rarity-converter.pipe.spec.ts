import { RarityConverterPipe } from './rarity-converter.pipe';

describe('RarityConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new RarityConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
