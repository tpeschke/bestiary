import { TypeConverterPipe } from './type-converter.pipe';

describe('TypeConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
