import { MoraleConverterPipe } from './morale-converter.pipe';

describe('MoraleConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new MoraleConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
