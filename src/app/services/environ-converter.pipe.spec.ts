import { EnvironConverterPipe } from './environ-converter.pipe';

describe('EnvironConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new EnvironConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
