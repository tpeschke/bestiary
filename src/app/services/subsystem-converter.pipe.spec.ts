import { SubsystemConverterPipe } from './subsystem-converter.pipe';

describe('SubsystemConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new SubsystemConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
